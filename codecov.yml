coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
    patch:
      default:
        target: auto
        threshold: 1%

parsers:
  javascript:
    enable_partials: false

ignore:
  - "**/*.d.ts"
  - "test-results"
  - "coverage"
  - "node_modules"