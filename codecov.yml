coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
    patch:
      default:
        target: auto
        threshold: 1%

parsers:
  javascript:
    enable_partials: false

ignore:
  - "src/index.ts"
  - "**/*.d.ts"
  - "test-results"
  - "coverage"
  - "node_modules"